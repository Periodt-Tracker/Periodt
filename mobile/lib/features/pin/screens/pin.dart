import 'package:flutter/material.dart';

class PinCheckScreen extends StatelessWidget {
  const PinCheckScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Pin Check Screen')));
  }
}
